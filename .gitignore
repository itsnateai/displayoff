# Task Management
AUDIT_TASKS.md
TODO_LIST.md
SCHEDULED_TASKS.md
ASSUMPTIONS.md
WORKFLOW_STATE.md
FINAL_REPORT.md

# Editor/OS artifacts
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# Temp files
*.tmp
*.bak
*.log

# Merge/patch artifacts
*.orig
*.rej

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Config (may contain personal paths)
*.cfg
displayoff_config.json

# Environment
.env

# Workspace-internal docs (LOCAL ONLY — never publish)
CLAUDE.md

# swarmaudit artifacts (audit cache, never publish)
.swarmaudit/

# Nuitka build artifacts (v1.7.13+ frozen .exe build dir + crash reports)
# build/ holds the displayoff.dist + displayoff.onefile-build + displayoff.exe
# from `bash build-release.sh` or `build-exe.bat`. The .exe gets uploaded
# as a release asset; the build dir is local-only scratch.
build/
nuitka-crash-report.xml
